@model Event
    <!-- Event Details Start -->
<div class="event-details-area blog-area pt-150 pb-140">
    <div class="container">
        <div class="row">
            <div class="col-md-8">
                <div class="event-details">
                    <div class="event-details-img">
                        <img src="~/assets/img/event/@Model.ImageUrl" alt="event-details">
                        <div class="event-date">
                            <h3>@Model.StartTime.Hour <span>@Model.StartTime.ToString("MMM")</span></h3>
                        </div>
                    </div>
                    <div class="event-details-content">
                        <h2>@Model.Title</h2>
                        <ul>
                            <li><span>time:</span> @Model.StartTime.ToString("HH") PM - @Model.EndTime.ToString("HH") PM</li>
                            <li><span>venue</span> @Model.Venue</li>
                        </ul>
                        @Html.Raw(Model.Content)
                        <div class="speakers-area fix">
                            <h4>speakers</h4>
                            @foreach (EventSpeaker eventSpeaker in Model.EventSpeakers)
                            {
                                <div class="single-speaker">
                                    <div class="speaker-img">
                                        <img src="~/assets/img/event/speaker1.jpg" alt="speaker">
                                    </div>
                                    <div class="speaker-name">
                                        <h5>Anthony Smith</h5>
                                        <p>CEO, Hastech </p>
                                    </div>
                                </div>

                            }
                        </div>
                    </div>
                    <div class="reply-area">
                        <h3>LEAVE A REPLY</h3>
                        <p>I must explain to you how all this a mistaken idea of ncing great explorer of the rut<br> the is lder of human happinescias unde omnis iste natus error sit volptatem </p>
                        <form id="contact-form" action="http://demo.devitems.com/eduhome/mail.php" method="post">
                            <div class="row">
                                <div class="col-md-12">
                                    <p>Name</p>
                                    <input type="text" name="name" id="name">
                                </div>
                                <div class="col-md-12">
                                    <p>Email</p>
                                    <input type="text" name="email" id="email">
                                </div>
                                <div class="col-md-12">
                                    <p>Subject</p>
                                    <input type="text" name="subject" id="subject">
                                    <p>Massage</p>
                                    <textarea name="message" id="message" cols="15" rows="10"></textarea>
                                </div>
                            </div>
                            <a class="reply-btn" href="#" data-text="send"><span>send message</span></a>
                            <p class="form-messege"></p>
                        </form>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="blog-sidebar right">
                    @await Component.InvokeAsync("Sidebar")
                </div>
            </div>
        </div>
    </div>
</div>
<!-- Event Details End -->